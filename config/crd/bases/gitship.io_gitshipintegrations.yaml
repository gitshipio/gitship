---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: gitshipintegrations.gitship.io
spec:
  group: gitship.io
  names:
    kind: GitshipIntegration
    listKind: GitshipIntegrationList
    plural: gitshipintegrations
    singular: gitshipintegration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitshipIntegration is the Schema for the gitshipintegrations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitshipIntegrationSpec defines the desired state of GitshipIntegration
            properties:
              config:
                additionalProperties:
                  type: string
                description: |-
                  Configuration for the integration
                  For cloudflare-tunnel: {"token": "...", "apps": ["myapp"]}
                type: object
              enabled:
                default: true
                description: Whether the integration is enabled
                type: boolean
              replicas:
                default: 1
                description: Desired number of replicas
                format: int32
                type: integer
              resources:
                description: Resource limits/requests
                properties:
                  cpu:
                    default: 500m
                    description: CPU limit (e.g. "500m", "1")
                    type: string
                  memory:
                    default: 1Gi
                    description: Memory limit (e.g. "512Mi", "1Gi")
                    type: string
                  storage:
                    default: 1Gi
                    description: Storage limit (e.g. "1Gi", "10Gi")
                    type: string
                type: object
              type:
                description: Type of integration (e.g. "cloudflare-tunnel")
                type: string
            required:
            - type
            type: object
          status:
            description: GitshipIntegrationStatus defines the observed state of GitshipIntegration
            properties:
              desiredReplicas:
                description: Desired number of pods
                format: int32
                type: integer
              message:
                description: Human-readable message
                type: string
              phase:
                description: '"Ready", "Error", "Pending", "Disabled"'
                type: string
              readyReplicas:
                description: Actual running pods
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
